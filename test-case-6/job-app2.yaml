apiVersion: batch/v1
kind: Job
metadata:
  name: job-app2
  namespace: default
  labels:
    applicationId: "job-app2"
    queue: "root.root2.groupA.childX.job-app2"
spec:
  completions: 5
  parallelism: 5
  template:
    metadata:
      labels:
        applicationId: "job-app2"
        queue: "root.root2.groupA.childX.job-app2"
    spec:
      restartPolicy: OnFailure
      containers:
        - name: worker
          image: busybox
          command: ["sh", "-c", "sleep 60"]
          resources:
            requests:
              cpu: "100m"
              memory: "100Mi"